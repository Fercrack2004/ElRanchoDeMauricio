<%# app/views/information_pages/index.html.erb %>
<div class="bg-gray-50">
  
  <%# --- Cabecera de la Sección --- %>
  <div class="bg-white border-b border-gray-200">
    <div class="container mx-auto px-4 py-12 text-center">
      <h1 class="text-4xl md:text-5xl font-serif font-bold text-primary-800">Consejos y Técnicas</h1>
      <p class="mt-4 text-lg text-gray-600 max-w-2xl mx-auto">Sumérgete en nuestros artículos sobre ingredientes, técnicas de cocina, y todo lo que necesitas para llevar tus habilidades al siguiente nivel.</p>
    </div>
  </div>

  <%# --- Cuadrícula de Artículos de Información --- %>
  <div class="container mx-auto px-4 py-12">
    <% if @information_pages.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        
        <%# Iteramos sobre la variable de instancia @information_pages %>
        <% @information_pages.each do |info_page| %>
          <%# Para cada uno, renderizamos el parcial de la tarjeta que ya creamos %>
          <%= render 'information_pages/info_card', information: info_page %>
        <% end %>

      </div>
    <% else %>
      <div class="text-center py-16">
        <p class="text-gray-500">Aún no hay artículos de información publicados.</p>
        <% if current_user&.moderator? || current_user&.admin? %>
          <%= link_to "Crear Nueva Página de Información", new_information_page_path, class: "mt-4 inline-block bg-primary-600 hover:bg-primary-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors shadow-md" %>
        <% end %>
      </div>
    <% end %>
  </div>

</div>